# Dependencies
node_modules

# Build artifacts (generated by `hytopia build` / `hytopia start`)
/index.mjs
*.zip

# Generated / cached assets — re-fetched & optimized from the
# @hytopia.com/assets npm package at runtime. Only map.json + ui/ are ours.
assets/blocks/
assets/models/
assets/audio/
assets/icons/
assets/particles/
assets/skyboxes/

# Local persisted player/global data (created by the dev server)
/dev/

# OS / editor
.DS_Store
*.log
backend/data.sqlite*

# Production deploy: secrets + built client (deploy/ scaffolding IS tracked)
deploy/.env
/client-dist/
